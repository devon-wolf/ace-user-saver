import { List } from '@material-ui/core';
import React from 'react';

const UserList = (): JSX.Element => {
    return (
        <List aria-label="saved users">
            
        </List>
    );
};

export default UserList;
